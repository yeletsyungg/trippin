# Мои заметки

## Введение в командную строку Bash

> Bash - это консоль (CLI) и скриптовый ЯП

### Управление Терминалом

Сдвинуть вверх лог выполненных ранее команд
`Crtl-L`

или с очисткой всего лога
```shell
clear
```
Сбросить настройки терминала и очищает экран
```shell
reset
```
Показать историю команд
```shell
history
```
Выполнить нужную коману из списка **History**
```shell
!53
```
где **53**  - это № команды из списка

Выполнить предыдущую команду
```shell
!!
```
Перелистывание ранее введённых команд
`Стрелка вверх/Стрелка вниз`

Автодополнение команд выполнятся по `TAB`

`Ctrl+C` - остановить запущенный в консоле процесс по

### Файловые операции

Получить структуру каталогов и файлов
```shell
tree
```
Показать абсолютный путь текущей директории
```shell
pwd
```
Показать содержимое текущего каталога
```shell
ls
```
Показать содержимое указанного каталога
```shell
ls dir_name
```
Показать подробное содержимое текущего каталога
```shell
ll
```
или
```shell
ls -all
```
включая скрытые файлы и папки

Примеры скрытых файлов и папок: `.gitignore` и `.git`

Показать подробное содержимое указанного каталога
```shell
ll dir_name
```
Показать содержимое в виде дерева
```shell
tree
```
Вернуться в домашний каталог текущего пользователя
```shell
cd ~
```
Вернуться в предыдущую папку
```shell
cd -
```

Знаки:
- `\` - обратный слэш (бэкслэш) - признак каталога, Windows
- `/` - прямой слэш - признак каталога, Unix/Linux/Android/macOS,iOS,BSD:
- `|` - вертикальный слэш - признак разделения командпользователя
- `~` - тильда, признак домашнего каталога текущего пользователя

Зайти в нужный каталог
```shell
cd dir_name
```
где `dir_name` - это имя нужного вам каталога

Выйти из текущего каталога на 1 шаг вверх
```shell
cd ..
```
Выйти из текущего каталога на 2 шага вверх
```shell
cd ../..
```

Правила именования каталогов и файлов:
1. Только по-английски
1. Без пробелов и спец.символов
1. Если имя файла или каталога состоит из нескольких англ.слов., то имя можно создать так: `firstOfall` или `first_of_all`
1. В имени файлов лучше всегда указывать расширение `fileName.txt`

> **Важно:** для программирования прописные и строчные буквы - это разные буквы. `w` и `W` - это разные буквы

#### Создание файлов и каталогов

Создание пустых файлов
```shell
touch file_name.txt
```
редактировать текстовый файл
```shell
micro file_name.txt
```
- `micro`- тектовый редактор командной строки
- Сохранить по `Ctrl+S` и выйти из редактора по `Ctrl+Q`

Переименовать файл
```shell
mv old_name new_name
```
копирование файла
```shell
cp name_file.txt NewFolder/
```
переместить файл
```shell
mv file.txt TargetName
```
Удалить файл
```shell
rm delFile.txt
```
Удалить папку
```shell
rm -rf DelFolder
```
Создать несколько файлов
```shell
touch {1..3}.txt
```
Создать несколько каталогов
```shell
mkdir folderName{1..4}
```
Удалить содержимое текущего каталога
```shell
rm -rf *
```
Удалить сразу несколько файлов по маске
```shell
rm {1..3}.js
```

Создать структуру проекта:
```
project/
├── css/
├── js/
├── img/
│   └── ico/
├── fonts/
└── pages/
```
Создать такую структуру одной командой:
```shell
mkdir -p project/{css,js,img/ico,fonts,pages}
```

### Linux

Показать версию Linux
```shell
lsb_release -a
```
Показать красивую ин-фу по системе
```shell
neofetch
```
Показать подробную ин-фу по системе
```shell
inxi -F
```
Показать Диспетчер задач
```shell
htop
```
Показать t CPU/GPU и скорость вентиляторов
```shell
sensors
```
Показать состояние оперативной памяти и подкачки (swap)
```shell
free -h
```
Показать ин-фу о текущем пользователе
```shell
w
```
или
```shell
id
```
Доступные группы
```shell
groups
```

### Софт

Календарь
```shell
cal
```
или на указанны год
```shell
cal 2026
```
Показать время
```shell
date
```

### Сеть и управление компьютером

Имя компьютера
```shell
hostname
```
Локальный ip
```shell
hostname -i
```
сетевая информация
```shell
ifconfig
```
или
```shell
ip -c r
```
или подробно
```shell
ip -c a
```
Пинг
```shell
ping ya.ru
```
или определённое кол-во раз
```shell
ping -c 4 ya.ru
```
Получить ин-фу по домену
```shell
whois ozon.ru
```
Получить порты
```shell
netstat -an
```
Получить маршруты
```shell
route
```
Выйти из сеанса пользователя
```shell
exit
```
Перезагрузить
```shell
reboot
```
Выключить
```shell
poweroff
```
или
```shell
sudo shutdown -r now
```
или
```shell
sudo systemctl reboot
```
Выключение
```shell
poweroff
```
или
```shell
sudo shutdown -p now
```
или
```shell
sudo systemctl poweroff
```

### Файловые операции

Создать пустой тектовый файл
```shell
touch emptyFile.txt
```
**Nano**
```shell
nano file_name.txt
```
Сохранить по **Ctrl+S**, выйти по **Ctrl+X**

```shell
micro file_name.txt
```
Сохранить по **Ctrl+S**, выйти по **Ctrl+Q**

Запустить Python-скрипт
```shell
python3 hello.py
```

Программа на C++
```cpp
#include <iostream>
//#include <unistd.h>
#include <thread>

int main() {
	puts("Hello\nЖдём 2 ~сек...");
	//usleep(2000'000);
	std::this_thread::sleep_for(std::chrono::seconds(2));
	return 0;
}
```
Скомпилировать код на C++
```shell
g++ main.cpp -o main.bin
```
Запустить бинарный файл
```shell
./main.bin
```
Показать используемые программой библиотеки
```shell
ldd ./main.bin
```
Покать время выполнения скрипта или программы
```shell
time ./main.bin
```
Показать таблицу ASCII
```shell
ascii -d
```

-b покажет двоичный код

Дополнительно: групповое создание файлов

Создать 10 файлов с именами от 1 до 10 с расширением .txt
```shell
touch {1..10}.txt
```
Создать пустой каталог
```shell
mkdir dir_name
```
Создать много папок одновременно
```shell
mkdir folder{1..3}
```
Удалить группу файлов по шаблону
```shell
rm -r folder{1..5}
```

Дополнительно: групповое создание структуры каталогов

Создать такую структуру:
```
project/
├── css/
├── js/
├── img/
│   └── ico/
├── fonts/
└── pages/
```
одной командой
```shell
mkdir -p project/{css,js,img/ico,fonts,pages}
```
Копировать файл в указанную папку
```shell
cp file1.txt folder1
```
Переместить файл в указанную папку
```shell
mv file2.txt folder2
```
Переименовать файл
```shell
mv file3.txt fileThree.txt
```

`mv старое_имя новое_имя`


Удаление файла
```shell
rm -i deleteFile
```
Удаление каталога
```shell
rm -ri dir_name
```

Удаление каталога без лишних вопросов
```shell
rm -rf dir_name
```

Удаление группы файлов по имени и расширению
```shell
rm -i {1..10}.txt
```


### Управление компьютером

Перезагрузка
```shell
reboot
```



> Если вы обнаружили ошибку в этом тексте - сообщите пожалуйста автору!
